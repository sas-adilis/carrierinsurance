console.log(prestashop.selectors.checkout);
/*prestashop.on('updatedDeliveryForm', (params) => {
    console.log('passe', params);
    if (
        typeof  params.deliveryOption !== 'undefined' &&
        typeof  params.deliveryOption.prevObject !== 'undefined' &&
        typeof  params.deliveryOption.prevObject[0] !== 'undefined' &&
        params.deliveryOption.prevObject[0].getAttribute('id') === 'carrier_insurance'
    ) {
        //document.location.reload();
    }
});*/